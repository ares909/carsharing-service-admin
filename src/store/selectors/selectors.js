export const apiData = (state) => state.api;
export const formData = (state) => state.form;
export const validationState = (state) => state.validation;
export const authState = (state) => state.auth;
